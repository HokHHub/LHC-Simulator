import { Plugin } from 'vite';

/**
 * ---------------------------------------------------------------------------
 *  Vite Plugin: vite-plugin-robots-txt
 *  - Emits robots.txt in build
 *  - Serves robots.txt in dev with no-store
 *  - Optional mirror to disk via outputDir (both dev & prod)
 *  - Unified style, Vite logger, no external helpers
 * ---------------------------------------------------------------------------
 */
/** One user-agent block */
type RobotsPolicy = {
    userAgent?: string;
    allow?: string[];
    disallow?: string[];
};
type RobotsOptions = {
    filename?: string;
    policies?: RobotsPolicy[];
    policyBuilder?: (ctx: {
        mode: string;
        command: "serve" | "build";
        root: string;
    }) => RobotsPolicy[];
    sitemaps?: string[] | ((ctx: {
        mode: string;
        command: "serve" | "build";
        root: string;
    }) => string[] | undefined);
    footerComment?: string | ((ctx: {
        mode: string;
        command: "serve" | "build";
        root: string;
    }) => string | undefined);
    noStoreInDev?: boolean;
    outputDir?: string;
};
declare function generateRobotsTxt(options?: RobotsOptions): Plugin;

// @ts-ignore
export = generateRobotsTxt;
export { type RobotsOptions, type RobotsPolicy, generateRobotsTxt };
